"""
Dashboard models.

The dashboard app is primarily read-only (aggregations).
No custom models are needed at this stage — stats are computed
from accounts and leaves data.

This file is kept as a placeholder for future dashboard-specific models
(e.g., announcements, pinned widgets, user preferences).
"""
